/*
operation										appearance		mnemonic
add																		+				add			.
subtract																-				sub			.
multiply																*				mul			.
divide																	/				div			.
negate																	‾				neg			.
exponentiate															^				pow			.
xth root																×√				xrt			.
equal to																=				eq			.
not equal to															≠				neq			.
greater than															>				gt			.
greater than or equal													≥				gte			.
less than																<				lt			.
less than or equal														≤				lte			.
store																	→				sto			.
factorial																!				fac			.
pi																		π				pi			.
percent																	%				prcnt		.
radian symbol															ʳ				fromRad		.
degree symbol															°				fromDeg		.
reciprocal																⁻¹				rcpr		.
square																	²				sqr			.
cube																	³				cube		.
square root																√(				sqrt		.
principal cube root														³√(				cbrt		.
1-PropZInt																1-PropZInt(		1PZInt		.
2-PropZInt																2-PropZInt(		2PZInt		.
1-PropZTest																1-PropZTest(	1PZTst		.
2-PropZTest																2-PropZTest(	2PZTst		.
2-SampFTest																2-SampFTest		2SFTst		.
2-SampTInt																2-SampTInt		2STInt		.
2-SampTTest																2-SampTTest		2STTst		.
2-SampZInt																2-SampZInt(		2SZInt		.
2-SampZTest																2-SampZTest(	2SZTst		.
1-Var Stats																1-Var Stats		1VStat		.
2-Var Stats																2-Var Stats		2VStat		.
absolute value															abs(			abs			.
logical and																and				and			.
complex angle															angle(			angle		.
analysis of variance													ANOVA(			ANOVA		.
answer																	Ans				ans			.
archive																	Archive			archv		.
concatenate lists														augment(		augm		.
set ans mode AUTO														AUTO Answer		autoAns		.
axes off																AxesOff			axOff		.
axes on																	AxesOn			axOn		.
cartesian complex mode													a+bi			ApBI		.
bg off																	BackgroundOff	bgOff		.
bg on																	BackgroundOn	bgOn		.
bal after n payments in amortization schedule							bal(			bal			.
binomial cdf															binomcdf(		binCDF		.
binomial pdf															binompdf(		binPDF		.
box plot																Boxplot			boxplot		.
check timer																checkTmr(		chkTmr		.
chi squared cdf															χ²cdf(			chi2CDF		.
chi squared pdf															χ²pdf(			chi2PDF		.
chi squared test														χ²-Test(		chi2Tst		.
chi squared GOF test													χ²GOF-Test(		chi2GOF		.
draw circle																Circle(			circle		.
classic print mode														CLASSIC			classic		.
clear entries															Clear Entries	clrEnts		.
clock off																ClockOff		clkOff		.
clock on																ClockOn			clkOn		.
clear all lists															ClrAllLists		clrAllLsts	.
clear drawings															ClrDraw			clrDraw		.
clear home																ClrHome			clrHome		.
clear list																ClrList			clrLst		.
clear table																ClrTable		clrTbl		.
conjugate																conj(			conj		.
draw graph as connected													Connected		conn		.
disable coordinate display												CoordOff		coordOff	.
enable coordinate display												CoordOn			coordOn		.
cosine																	cos(			cos			.
inverse cosine															cos⁻¹(			acos		.
hyperbolic cosine														cosh(			cosh		.
inverse hyperbolic cosine												cosh⁻¹(			acosh		.
cubic polynomial regression												CubicReg		cubReg		.
cumulative sum															cumSum(			cumSum		.
day of week																dayOfWk(		dayOfWk		.
days between dates														dbd(			dbd			.
decimal answers mode													DEC Answer		decAns		.
to decimal																►Dec			toDec		.
set degree mode															Degree			degMode		.
delete variable															DelVar			delVar		.
don't auto-populate dependent cells										DependAsk		depAsk		.
auto-populate dependent cells											DependAuto		depAuto		.
determinant																det(			det			.
don't detect asymptotes													DetectAsymOff	detAsymoff	.
detect asymptotes														DetectAsymOn	detAsymon	.
disable correlation stats r and R²										DiagnosticOff	diagOff		.
enable correlation stats r and R²										DiagnosticOn	diagOn		.
size of list or matrix													dim(			dim			.
display																	Disp			disp		.
display graph															DispGraph		dispGr		.
display table															DispTable		dispTbl		.
convert to days, minutes, and seconds									►DMS			toDMS		.
set dotted line style (not connected)									Dot				dot			.
set thick dotted line style												Dot-Thick		dotThick	.
set thin dotted line style												Dot-Thin		dotThin		.
draw expression in terms of x											DrawF			drawF		.
draw inverse of expression in terms of x								DrawInv			drawInv		.
decrement and skip if less than											DS<(			decSkLt		.
e (euler's number)														e				e			.
e to a power (exp)														e^(				exp			.
scientific notation														E				sciNot		.
convert nominal to effective interest rate								►Eff(			toEff		.
else block																Else			else		.
end block																End				end			.
engineering notation mode												Eng				engMode		.
equation to string														Equ►String(		equToStr	.
real to string															eval(			eval		.
execute library															ExecLib			execLib		.
string to expression													expr(			expr		.
exponential regression													ExpReg			expReg		.
disable expression display in graph										ExprOff			exprOff		.
enable expression display in graph										ExprOn			exprOn		.
f-distribution cdf														Fcdf(			fCDF		.
convert between fraction and decimal forms								►F◄►D			convFD		.
fill list or matrix with value											Fill(			fill		.
fixed point display mode												Fix				fixMode		.
floating point display mode												Float			floatMode	.
function max															fMax(			fMax		.
function min															fMin(			fMin		.
definite integral of function											fnInt(			fnInt		.
disable an equation in the graph										FnOff			fnOff		.
enable an equation in the graph											FnOn			fnOn		.
for block																For(			for			.
fractional part															fPart(			fPart		.
f-distribution pdf														Fpdf(			fPDF		.
fractional answer mode													FRAC Answer		fracAns		.
to fraction																►Frac			toFrac		.
set screen mode to full													Full			fullMode	.
set function graphing mode												Func			funcMode	.
reclaim archive space marked as deleted									GarbageCollect	garbCollect	.
greatest common denominator												gcd(			gcd			.
cumulative geometric probability										geometcdf(		geoCDF		.
geometric probability													geometpdf(		geoPDF		.
get variable from connected device										Get(			get			.
get variable from connected calculator									GetCalc(		getCalc		.
get current date														getDate			getDate		.
get date format															getDtFmt		getDtFmt	.
get current date as a string											getDtStr(		getDtStr	.
get last key pressed													getKey(			getKey		.
get current time														getTime			getTime		.
get time format															getTmFmt		getTmFmt	.
get current time as a string											getTmStr(		getTmStr	.
goto label																Goto			goto		.
set graph color															GraphColor(		graphColor	.
set graph style															GraphStyle(		graphStyle	.
turn off grid															GridOff			gridOff		.
turn on grid															GridOn			gridOn		.
graph table mode														G-T				grTblMode	.
histogram																Histogram		histogram	.
horizontal mode															Horiz			horizMode	.
draw horizontal line													Horizontal		horizontal	.
imaginary number i														i				i			.
identity matrix															identity(		identity	.
if block																If				if			.
imaginary part															imag(			imag		.
don't auto-populate independent cells									IndpntAsk		indpntAsk	.
auto-populate independent cells											IndpntAuto		indpntAuto	.
get input																Input			input		.
index of substring in string											inString(		inStr		.
floor																	int(			int			.
calculate interest in amortization schedule								ΣInt(			sigmaInt	.
inverse normal cdf														invNorm(		invNorm		.
inverse t-dist cdf														invT(			invT		.
integer part/truncate													iPart(			iPart		.
internal rate of return													irr(			irr			.
increment and skip if greater											IS>(			incSkGt		.
check if clock is on													IsClockOn		isClkOn		.
list in var																L				lst			.
disable labels on coord axes											LabelOff		lblOff		.
enable labels on coord axes												LabelOn			lblOn		.
basic label																Lbl				lbl			.
least common multiple													lcm(			lcm			.
token length of string													length(			length		.
draw line																Line(			line		.
calculate linear regression	as a+bx										LinReg(a+bx)	linRegABX	.
calculate linear regression as ax+b										LinReg(ax+b)	linRegAXB	.
calc linreg with t confidence interval									LinRegTInt		linRegTI	.
calc linreg with t-test													LinRegTTest		linRegTT	.
calc deltas in list														DeltaList(		deltaList	.
convert lists to matrix													List►matr(		lstToMat	.
natural logarithm														ln(				ln			.
logarithmic regression													LnReg			lnReg		.
base-10 logarithm														log(			log10		.
base-k logarithm														logBASE(		logB		.
logistic regression														Logistic		logistic	.
manual linear regression												Manual-Fit		manFit		.
mathprint mode															MATHPRINT		mathprint	.
matrix to lists															Matr►list(		matToLst	.
max of args or list														max(			max			.
mean of list															mean(			mean		.
median of list															median(			median		.
median fit line															Med-Med			medMed		.
create menu																Menu(			menu		.
min of args or list														min(			min			.
modified boxplot														ModBoxplot		modBoxplot	.
combinations															nCr				nCr			.
fraction																n/d				mkFrac		.
derivative at point														nDeriv(			nDeriv		.
convert between improper and mixed fraction								►n/d◄►Un/d		convImMx	.
effective to nominal interest rate										►Nom(			toNom		.
normal number mode														Normal			normMode	.
normal dist cdf															normalcdf(		normCDF		.
normal dist pdf															normalpdf(		normPDF		.
normal prob plot														NormProbPlot	normPrPlt	.
logical not																not(			not			.
permutations															nPr				nPr			.
net present value														npv(			npv			.
open library															OpenLib(		openLib		.
logical or																or				or			.
output text at position													Output(			output		.
enable parametric graphing mode											Param			paramMode	.
wait for enter press													Pause			pause		.
piecewise expression													piecewise(		piecewise	.
define and enable plot 1												Plot1(			plot1		.
define and enable plot 2												Plot2(			plot2		.
define and enable plot 3												Plot3(			plot3		.
disable plots															PlotsOff		pltsOff		.
enable plots															PlotsOn			pltsOn		.
assume payments made at beginning of periods							Pmt_Bgn			pmtBgn		.
assume payments made at end of periods									Pmt_End			pmtEnd		.
poisson cdf																poissoncdf(		poiCDF		.
poisson pdf																poissonpdf(		poiPDF		.
enable polar graphing mode												Polar			polarMode	.
convert to polar														►Polar			toPolar		.
polar grid coordinate mode												PolarGC			polarGCMode	.
execute program															prgm			prgm		.
calc principal amount over range of payments in amortization schedule	ΣPrn(			sigmaPrn	.
product of list elements												prod(			prod		.
prompt for input														Prompt			prompt		.
toggle point on graph													Pt-Change(		ptChg		.
turn off point on graph													Pt-Off(			ptOff		.
turn on point on graph													Pt-On(			ptOn		.
exponential regression													PwrReg			pwrReg		.
toggle pixel on graph													Pxl-Change(		pxlChg		.
turn off pixel on graph													Pxl-Off(		pxlOff		.
turn on pixel on graph													Pxl-On(			pxlOn		.
test pixel on graph														pxl-Test(		pxlTest		.
polar to cartesian x													P►Rx(			pToRx		.
polar to cartesian y													P►Ry(			pToRy		.
quadratic regression													QuadReg			quadReg		.
quartic regression														QuartReg		quartReg	.
set angle mode to radians												Radian			radian		.
generate uniform random numbers											rand(			rand		.
generate random numbers according to a binomial distribution			randBin(		randBin		.
generate uniform random integers within a range							randInt(		randInt		.
generate uniform random integers within a range, without repetition		randIntNoRep(	randIntNR	.
generate uniform random integers from -9 to 9 in a matrix				randM(			randM		.
generate normally distributed random numbers							randNorm(		randNorm	.
paste value of variable into text input field							Rcl				recall		.
set complex number mode to polar										re^θi			pCmplxMode	.
switch to real-only mode												Real			realMode	.
real part of complex number												real(			real		.
recall graph settings from graph database								RecallGDB		recallGDB	.
recall and draw picture on graph										RecallPic		recallPic	.
convert to rectilinear/cartesian coordinates							►Rect			toRect		.
use rectilinear grid coordinates on the graph screen					RectGC			rectGC		.
convert matrix with cols >= rows to row echelon form					ref(			rowEchForm	.
remainder																remainder(		remainder	.
repeat block															Repeat			repeat		.
return from program or subprogram										Return			return		.
round half-up															round(			round		.
multiply row of matrix by a scalar and return the result				*row(			multRow		.
add one row to another and return the result							row+(			rowAdd		.
add multiple of one row to another and return the result				*row+(			multRowAdd	.
swap 2 rows and return the result										rowSwap(		rowSwap		.
convert matrix with cols >= rows to reduced row echelon form			rref(			redREForm	.
cartesian to polar radius												R►Pr(			rToPr		.
cartesian to polar angle												R►Pθ(			rToPa		.
scatter plot															Scatter			scatter		.
scientific notation mode												Sci				sciMode		.
user selects points within bounds										Select(			select		.
send data to another device												Send(			send		.
create list from expression and sequence of inputs						seq(			seq			.
enable sequence graphing mode											Seq				seqMode		.
enable sequential plotting of graphs									Sequential		seqPltMode	.
set date																setDate(		setDate		.
set date format															setDtFmt(		setDtFmt	.
set time																setTime(		setTime		.
set time format															setTmFmt(		setTmFmt	.
set up list editor														SetUpEditor		setUpEditor	.
shade the area between two functions									Shade(			shade		.
shade an interval of the chi squared distribution						Shadeχ²(		shadeChi2	.
shade an interval of the F distribution									ShadeF(			shadeF		.
shade an interval of the normal distribution							ShadeNorm(		shadeNorm	.
shade an interval of the t distribution									Shade_t(		shadeT		.
simultaneous graphing mode												Simul			simul		.
sine																	sin(			sin			.
inverse sine															sin⁻¹(			asin		.
hyperbolic sine															sinh(			sinh		.
inverse hyperbolic sine													sinh⁻¹(			asinh		.
sinusoidal regression													SinReg			sinReg		.
solve equation in one variable											solve(			solve		.
sort list in ascending order											SortA(			sortA		.
sort list in descending order											SortD(			sortD		.
start timer																startTmr(		startTmr	.
disable stat wizard														STATWIZARD OFF	statWizOff	.
enable stat wizard														STATWIZARD ON	statWizOn	.
sample standard deviation												stdDev(			stdDev		.
stop program															Stop			stop		.
store graph settings in graph database									StoreGDB		storeGDB	.
store current graph state as an image									StorePic		storePic	.
store string in expression variable										String►Equ(		strToEqu	.
get substring															sub(			sub			.
sum of list elements													sum(			sum			.
sum in sigma notation													Σ(				sigmaSum	.
tangent																	tan(			tan			.
inverse tangent															tan⁻¹(			atan		.
draw function and line tangent to it at a point							Tangent(		tangent		.
hyperbolic tangent														tanh(			tanh		.
inverse hyperbolic tangent												tanh⁻¹(			atanh		.
t distribution cdf														tcdf(			tCDF		.
display small text
set text color
if block
use thick lines
use thin lines
use value over time sequence graphs
convert seconds to days, hours, minutes, and seconds
confidence interval for mean value of a population
convert to string
t distribution pdf
switch to trace mode
t significance test
time value of money solver
move from archive to ram
create mixed fraction
switch to (u,v) sequence graphing mode
switch to (u,w) sequence graphing mode
sample variance of list
draw vertical line
switch to (v,w) sequence graphing mode
wait for a specified number of seconds
switch to web diagram sequence graphing mode

*/